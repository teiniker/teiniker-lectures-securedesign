Example: HttpClient + TLS + JSON Mapping (BookService)
-------------------------------------------------------------------------------

Setup:

$ cd api-tls/SpringBoot-BookService-TLS 
$ mvn spring-boot:run 

$ curl -ik https://localhost:8443/books

